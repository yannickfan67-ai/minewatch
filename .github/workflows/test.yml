name: minewatch-test
on: [push, pull_request]

jobs:
  room-boundaries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run dependency-free room tests
        run: npm test
